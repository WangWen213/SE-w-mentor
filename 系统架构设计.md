# SE-Mentor 系统架构设计

> 文档：`docs/ARCHITECTURE.md`  
> 对应任务：T111  
> 系统：SE-Mentor — Software Change Proposal Analysis and Governance  
> 主要交付形态：Windows 本地 Coding Agent Harness + React WebUI + FastAPI + SQLite  
> 核心原则：**先理解、先分析、先治理，再执行**

---

# 1. 文档目的

SE-Mentor 是一个面向真实软件维护场景的 Coding Agent Harness。

它的目标并不是让大语言模型更直接、更快速地修改代码，而是在 LLM 和真实代码仓库之间建立一个独立的工程控制层，使代码变更过程具备：

- 项目理解能力；
    
- 软件演化记忆；
    
- 变更方案确认；
    
- 影响分析；
    
- 风险治理；
    
- 权限约束；
    
- 安全执行；
    
- 客观验证；
    
- 失败修正；
    
- 回滚与恢复；
    
- 全过程审计。
    

SE-Mentor 不采用：

```text
用户需求
↓
LLM
↓
直接修改代码
```

而采用：

```text
用户需求
↓
项目理解
↓
修改方案 Proposal
↓
用户确认
↓
影响分析
↓
治理决策
↓
ExecutionPolicy
↓
受约束执行
↓
验证
↓
必要时修正
↓
完成
↓
工程知识更新
```

因此，SE-Mentor 的核心价值不是“生成代码”，而是：

> 将项目知识、代码证据、风险分析和用户授权转化为机器可执行的安全边界，并在真实代码写入前强制执行这些边界。

---

# 2. 总体设计原则

## 2.1 后端是业务状态的唯一权威

React WebUI 只负责：

- 展示状态；
    
- 接收用户输入；
    
- 发起 REST 请求；
    
- 接收 SSE 事件；
    
- 提供确认、审批、取消、回滚等交互入口。
    

以下状态必须由后端 Harness 管理：

- Project 生命周期；
    
- Task 生命周期；
    
- Proposal 状态；
    
- ImpactReport；
    
- GovernanceDecision；
    
- Approval；
    
- ExecutionPolicy；
    
- WRITE Lock；
    
- Transaction；
    
- Validation；
    
- Recovery；
    
- EngineeringKnowledge。
    

前端不得自行推导或伪造这些业务状态。

---

# 3. 总体系统架构

```text
┌─────────────────────────────────────────────┐
│                    用户                     │
└─────────────────────┬───────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│            React + TypeScript WebUI         │
│                                             │
│ 工作台 / 任务 / 记忆 / 治理 / 评估         │
└─────────────────────┬───────────────────────┘
                      │ REST + SSE
                      ▼
┌─────────────────────────────────────────────┐
│                 FastAPI API                 │
└─────────────────────┬───────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│             SE-Mentor Agent Harness         │
│                                             │
│ Project / Task / Proposal                   │
│ Context / Code Index / Knowledge            │
│ Impact Analysis                             │
│ Governance / Approval                       │
│ ExecutionPolicy                             │
│ Agent Orchestrator                          │
│ Dispatcher / PolicyEnforcer                 │
│ Lock / Transaction / Recovery               │
│ Validation / Feedback / CompletionGate      │
└─────────────┬─────────────────┬─────────────┘
              │                 │
              ▼                 ▼
         SQLite DB          LLM Provider
              │
              │
              ▼
     本地 Git Repository
              │
              ▼
       Build / Test / Git
```

---

# 4. 核心 Harness 主链路

SE-Mentor 的完整工程链为：

```text
Local Project
      ↓
Project Registration
      ↓
Project Bootstrap
      ↓
Code Index + Git + Engineering Knowledge
      ↓
Task
      ↓
Change Proposal
      ↓
User Confirmation
      ↓
ContextPackage
      ↓
Impact Analysis
      ↓
Governance
      ↓
Approval（必要时）
      ↓
ExecutionPolicy
      ↓
LLM
      ↓
AgentAction
      ↓
Dispatcher
      ↓
PolicyEnforcer
      ↓
Transaction
      ↓
Tools
      ↓
Code Changes
      ↓
ValidationPlan
      ↓
Validation
      ↓
Failure Classification
      ↓
Feedback
      ↓
Repair Loop
      ↓
必要时重新治理
      ↓
CompletionGate
      ↓
Knowledge Update
```

这条链路同时体现了 SE-Mentor 的五个核心能力：

```text
理解
+
记忆
+
治理
+
执行
+
反馈
```

---

# 5. Project 与 Bootstrap 架构

## 5.1 Project Registration

用户打开本地 Git 项目后，系统首先注册项目。

Project 保存：

- 项目标识；
    
- 本地路径；
    
- Git 基线；
    
- 当前分支；
    
- 当前 revision；
    
- bootstrap 状态；
    
- 项目级配置。
    

项目之间必须严格通过 `projectId` 隔离。

一个项目的：

- Task；
    
- Memory；
    
- Governance；
    
- Evaluation；
    
- Recovery；
    
- Lock；
    

不得泄漏到其他项目。

---

## 5.2 Bootstrap

打开项目后，系统进行工程理解。

Bootstrap 主要产生：

```text
Repository Inventory
+
Code Symbols
+
Symbol Relations
+
Dependency Information
+
Git Baseline
+
Project Understanding
```

Project Bootstrap 的状态可以表示为：

```text
REGISTERED
     ↓
BOOTSTRAPPING
     ↓
READY

或

BOOTSTRAP_FAILED
```

这里的关键设计原则是：

> 项目是否 READY 必须由后端持久化工程知识和实际状态决定，不能由浏览器本地缓存决定。

---

# 6. Proposal 架构

用户输入自然语言需求后，SE-Mentor 不立即执行。

首先进入 Proposal 阶段。

```text
User Request
      ↓
ProposalContextBuilder
      ↓
Repository Evidence
+
Code Index
+
Engineering Knowledge
+
Git Information
      ↓
LLM Provider
      ↓
Structured Proposal
```

Proposal 需要描述：

- 用户目标；
    
- 修改范围；
    
- 预计涉及文件或模块；
    
- 主要修改内容；
    
- 潜在风险；
    
- 需要进一步确认的信息。
    

Proposal 使用结构化输出，而不是依赖自由文本解析。

---

# 7. 用户确认边界

Proposal 是“建议修改什么”。

User Confirmation 是：

> 用户是否同意以这个范围继续进行影响分析和治理。

确认 Proposal 并不意味着：

```text
允许 Agent 任意修改仓库
```

它只是授权进入后续治理阶段。

如果用户在确认后显著扩大需求范围，则原 Proposal 和相关权限不得继续沿用。

系统需要重新执行：

```text
Proposal
→ Impact
→ Governance
→ Confirmation / Approval
```

---

# 8. ContextPackage

ContextPackage 是 Agent 执行前统一的软件工程上下文。

它可能包含：

- 用户需求；
    
- 当前 Proposal；
    
- 相关代码符号；
    
- 相关文件；
    
- Git 基线；
    
- 依赖关系；
    
- 工程知识；
    
- ImpactReport；
    
- GovernanceDecision；
    
- ExecutionPolicy；
    
- Validation Feedback。
    

ContextPackage 的作用是防止 Agent 直接面对整个仓库进行无边界搜索。

---

# 9. 软件演化记忆架构

SE-Mentor 的 Memory 不是聊天记录。

系统保存的是：

> 可以被未来软件修改任务重新利用的软件工程知识。

主要组件包括：

```text
EngineeringKnowledgeService
KnowledgeRepository
KnowledgeRetriever
KnowledgeFreshnessChecker
KnowledgeExtractor
```

典型知识包括：

- 项目整体结构；
    
- 模块职责；
    
- API 契约；
    
- 关键依赖；
    
- 测试方式；
    
- 架构约束；
    
- 历史修改结果；
    
- 已验证的工程事实。
    

---

## 9.1 Knowledge 生命周期

```text
任务完成
↓
提取候选知识
↓
关联代码和验证证据
↓
生成 KnowledgeSignature
↓
CANDIDATE
↓
证据验证
↓
VERIFIED
```

知识并不是永久有效。

Git revision 或相关代码变化后，需要进行 Freshness Check。

可能的状态包括：

```text
FRESH
DRIFTED
STALE
```

因此：

> 历史事实不能无条件覆盖当前代码事实。

---

# 10. Impact Analysis 架构

用户确认 Proposal 后，系统需要确定：

> 如果执行这次修改，会影响什么？

Impact Analyzer 结合：

- Proposal；
    
- Code Index；
    
- Symbol Relations；
    
- Git；
    
- EngineeringKnowledge；
    
- Repository Evidence；
    

生成 `ImpactReport`。

ImpactReport 主要描述：

- 直接影响文件；
    
- 相关模块；
    
- 依赖传播；
    
- 测试影响；
    
- 数据模型影响；
    
- 配置影响；
    
- 风险点；
    
- 需要重点验证的区域。
    

ImpactReport 不直接决定是否允许执行。

它是 Governance 的输入。

---

# 11. Governance 架构

治理阶段解决的问题是：

> 这个修改可以执行吗？如果可以，允许执行到什么范围？

主要组件：

```text
ImpactAnalyzer
GovernanceEngine
RuleRepository
ConflictResolver
ApprovalService
ExecutionPolicyCompiler
PolicyEnforcer
```

治理输入包括：

```text
用户请求
Proposal
ImpactReport
AgentAction
代码证据
工程知识
项目配置
系统规则
已有审批
```

治理输出包括：

```text
GovernanceDecision
ExecutionPolicy
ApprovalRequest
RuleHit[]
```

---

# 12. 治理决策

核心决策分为：

```text
ALLOW
WARN / REQUIRE_APPROVAL
BLOCK
```

规则优先级为：

```text
DENY_HARD
    >
REQUIRE_APPROVAL
    >
ALLOW
```

因此如果同一个动作同时命中：

```text
ALLOW
+
DENY_HARD
```

最终结果必须为：

```text
BLOCK
```

用户普通审批不得覆盖 `DENY_HARD`。

---

# 13. ExecutionPolicy

GovernanceDecision 不是自然语言提示。

它必须进一步编译为机器可执行的 `ExecutionPolicy`。

ExecutionPolicy 可以约束：

```text
readable_paths
writable_paths
protected_paths
allowed_commands
denied_commands
network_allowed
expiration
task_id
project_id
proposal_scope
```

因此治理从：

```text
“这个修改风险较高”
```

进一步变成：

```text
“这个 Agent 只能修改这些文件，
只能运行这些命令，
不能访问这些目录，
不能访问网络，
权限仅对当前 Task 有效。”
```

---

# 14. ExecutionPolicy 双层强制

SE-Mentor 不允许治理只停留在 UI 或 Agent Prompt。

必须执行两层检查。

## 第一层：GovernanceEngine

```text
AgentAction
↓
GovernanceEngine
↓
ALLOW / WARN / BLOCK
```

它负责判断某个动作从治理语义上是否可以继续。

## 第二层：PolicyEnforcer

真正调用工具之前：

```text
Dispatcher
↓
PolicyEnforcer
↓
检查 tool + arguments + path + command
↓
允许后才进入 Tool
```

因此即使：

- LLM 生成了越权动作；
    
- Agent Orchestrator 出现逻辑错误；
    
- 前端状态异常；
    

工具层仍不能越过 ExecutionPolicy。

---

# 15. Agent Orchestrator

Agent Orchestrator 是 SE-Mentor 自研 Harness 主循环。

它负责：

- 管理任务状态；
    
- 构造 Agent Context；
    
- 调用 LLM；
    
- 解析 AgentAction；
    
- 将 Action 发送给 Dispatcher；
    
- 接收 ToolResult；
    
- 触发验证；
    
- 将反馈重新加入 Context；
    
- 控制修正循环；
    
- 检测停滞；
    
- 判断 CompletionGate；
    
- 处理取消和失败。
    

外部 Agent 框架不得替代这一主循环。

---

# 16. AgentAction

LLM 不允许直接输出：

```text
“运行 rm -rf ...”
```

并由系统原样执行。

LLM 输出必须被解析为结构化 AgentAction。

例如：

```text
READ_FILE
WRITE_FILE
PATCH_FILE
RUN_COMMAND
GIT_DIFF
RUN_TEST
```

每个 Action 必须经过：

```text
ActionParser
↓
Governance
↓
Dispatcher
↓
PolicyEnforcer
↓
Tool
```

---

# 17. Tool 架构

Tool 是 Harness 与外部环境之间唯一受控执行入口。

典型工具包括：

- File Tool；
    
- Patch Tool；
    
- Shell Tool；
    
- Git Tool；
    
- Validation Tool。
    

核心约束：

1. LLM 不得直接操作文件系统；
    
2. LLM 不得直接启动系统命令；
    
3. Shell 必须具有 timeout；
    
4. 工具参数必须经过路径和策略检查；
    
5. 有副作用的 Tool 必须进入 Transaction；
    
6. ToolResult 必须结构化；
    
7. Tool 调用必须进入审计记录。
    

---

# 18. WRITE Lock

审批只代表：

```text
“允许这个 Task 在指定范围写”
```

它不代表：

```text
“当前 Task 已获得仓库写入权”
```

真正产生副作用前，还需要获得项目级 WRITE Lock。

因此执行条件是：

```text
Governance Authorization
+
ExecutionPolicy
+
WRITE Lock
+
Transaction
```

同一个项目不能同时存在多个不受控制的 Writer。

---

# 19. Transaction 架构

所有写操作必须关联 Transaction。

典型状态：

```text
PREPARED
↓
APPLYING
↓
APPLIED
↓
VALIDATED
↓
COMMITTED
```

如果失败：

```text
ROLLBACK
```

Transaction Manifest 记录：

- 创建了什么文件；
    
- 修改了什么文件；
    
- 删除了什么文件；
    
- 修改前 Hash；
    
- 修改后 Hash；
    
- Backup；
    
- Base Revision。
    

---

# 20. Rollback

Rollback 不是简单运行：

```text
git reset --hard
```

而是依据本次 Transaction Manifest 逆序恢复。

```text
CREATE
→ 删除本次创建文件

MODIFY
→ 恢复 backup

DELETE
→ 恢复原文件
```

恢复前需要重新检查当前 Hash。

如果任务执行后用户又手动修改了同一个文件：

```text
Agent 修改
↓
用户修改
↓
Rollback
```

系统不能覆盖用户的新修改。

此时需要进入：

```text
Conflict / Manual Recovery
```

---

# 21. Crash Recovery

如果 SE-Mentor 在执行过程中异常退出：

```text
PREPARED / APPLYING Transaction
↓
程序退出
↓
重新启动
↓
扫描未完成 Transaction
↓
RECOVERY_REQUIRED
```

进入恢复状态后：

- 阻止新的 WRITE Task；
    
- 检查当前工作区；
    
- 比较 Manifest 与文件 Hash；
    
- 判断能否自动恢复；
    
- 存在冲突时要求用户处理。
    

---

# 22. Validation 架构

代码写入不是任务完成条件。

执行后必须进入 Validation。

```text
Code Change
↓
ValidationPlan
↓
ValidationTool
↓
Result
↓
FailureClassifier
```

可使用：

- 单元测试；
    
- 集成测试；
    
- 类型检查；
    
- 静态分析；
    
- Build；
    
- Git diff；
    
- 项目专用检查。
    

---

# 23. Feedback Loop

如果验证失败：

```text
Validation Failed
↓
Failure Classification
↓
Compressed Feedback
↓
Agent Context
↓
LLM
↓
Repair Action
↓
再次验证
```

发送给 LLM 的不是全部原始日志，而是压缩后的有效反馈，例如：

- 失败测试；
    
- 错误类别；
    
- 关键堆栈；
    
- 文件位置；
    
- 断言差异；
    
- 上次修改摘要。
    

---

# 24. Stagnation Detection

系统必须防止 Agent 无限循环。

典型停滞信号：

- 重复执行相同 Action；
    
- 同一测试连续相同失败；
    
- diff 长时间无有效变化；
    
- repair loop 没有改善；
    
- 连续多个 iteration 没有产生进展。
    

触发后：

```text
STAGNATION
↓
REPLAN
```

如果仍无法解决：

```text
BLOCK / STOP
```

---

# 25. CompletionGate

Agent 不允许自行说：

> “任务已经完成。”

最终完成状态必须由 CompletionGate 判断。

CompletionGate 综合：

```text
Proposal 是否满足
+
修改是否在授权范围
+
Validation 是否通过
+
是否存在未处理失败
+
是否存在未完成事务
+
是否存在治理阻断
```

满足条件后才能进入：

```text
COMPLETED
```

---

# 26. WebUI 架构

React WebUI 主要包括：

```text
工作台
任务
记忆
治理
评估
```

## 工作台

承担：

- 项目切换；
    
- 用户需求输入；
    
- Proposal；
    
- 确认；
    
- Agent 进度；
    
- 代码改动结果。
    

## 任务

展示：

- 历史任务；
    
- 当前状态；
    
- 时间线；
    
- 完成或失败结果。
    

## 记忆

展示：

- 项目理解；
    
- 工程知识；
    
- 证据；
    
- Freshness。
    

## 治理

展示每次治理记录：

```text
Task
Risk
Impact
Decision
Rule Hits
Approval
Execution Scope
```

治理记录按任务长期保留，而不是只展示当前一次。

## 评估

展示：

- Validation；
    
- 测试；
    
- 修改范围；
    
- 质量结果；
    
- Completion 判断。
    

---

# 27. 数据持久化

SQLite 是本地版本的结构化状态存储。

主要持久化：

- Projects；
    
- Tasks；
    
- Proposals；
    
- ImpactReports；
    
- GovernanceDecisions；
    
- Rules；
    
- Approvals；
    
- Execution Policies；
    
- Transactions；
    
- Validation；
    
- Engineering Knowledge；
    
- Code Index；
    
- Audit Events。
    

SQLite 不保存真实 LLM API Key。

---

# 28. 本地运行拓扑

完整本地版本运行方式：

```text
Windows 用户
      ↓
SE-Mentor
      ↓
FastAPI @ 127.0.0.1
      ↓
React WebUI
      ↓
本地 Harness
      ↓
用户 Git Repository
```

默认服务绑定：

```text
127.0.0.1
```

而不是：

```text
0.0.0.0
```

以减少本地 Harness 暴露到局域网或公网的风险。

---

# 29. LLM Provider 边界

SE-Mentor 支持 Provider 抽象。

```text
Harness
↓
LLM Gateway
↓
Provider Adapter
↓
OpenAI / Mock Provider
```

真实 Provider 用于本地完整模式。

Mock Provider 用于：

- 离线测试；
    
- 确定性 E2E；
    
- CI；
    
- 公共 Demo；
    
- 无 Key 场景。
    

Provider 不能控制工具执行。

它只能产生模型输出。

---

# 30. 自研 Harness 边界

SE-Mentor 可以使用：

- HTTP SDK；
    
- OpenAI SDK；
    
- FastAPI；
    
- SQLAlchemy；
    
- React；
    
- pytest；
    
- Git；
    
- 系统工具。
    

但不得使用现成 Coding Agent Runner 直接替代：

```text
Agent Orchestrator
Dispatcher
Governance
PolicyEnforcer
Transaction
Validation Feedback Loop
```

原因是这些机制本身就是 SE-Mentor 项目的核心工程实现和研究对象。

---

# 31. 异常路径

正常路径之外还存在：

```text
Execution
   ├── CANCEL → KEEP
   ├── CANCEL → ROLLBACK
   ├── CRASH → RECOVERY
   ├── STAGNATION → REPLAN
   └── BLOCK → STOP
```

因此 SE-Mentor 的状态机设计必须同时考虑：

```text
成功路径
+
失败路径
+
人为取消
+
程序崩溃
+
外部修改冲突
```

---

# 32. 架构最终结论

SE-Mentor 的核心架构不是：

```text
LLM + Tools
```

而是：

```text
                Engineering Knowledge
                       ↓
User
 ↓
Proposal
 ↓
Impact
 ↓
Governance
 ↓
ExecutionPolicy
 ↓
Agent Orchestrator
 ↓
Dispatcher
 ↓
PolicyEnforcer
 ↓
Transaction
 ↓
Tools
 ↓
Validation
 ↓
Feedback
 ↓
CompletionGate
```

其中 LLM 只是受 Harness 控制的推理组件。

真正负责：

- 权限；
    
- 状态；
    
- 写入；
    
- 风险；
    
- 回滚；
    
- 验证；
    
- 完成判断；
    

的是 SE-Mentor 自研 Harness。

这也是系统区别于“直接调用 LLM 修改代码”的核心架构边界。