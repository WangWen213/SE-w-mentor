# T003 TDD Evidence

Status: `[x]`.

## Classification Update

The frontend Vitest/Vite failure observed inside the Codex sandbox is now classified as
`CODEX_SANDBOX_NATIVE_CHILD_RESTRICTION`, not as a repository defect.

External evidence supplied by the user:

- `source = USER_EXECUTED_EXTERNAL_ENVIRONMENT`
- `privilege = ordinary_non_admin`
- working directory: `C:\Users\ww\Desktop\SE-w-mentor\frontend`
- `npm.cmd run test -- --run`: passed, 1 file and 1 test
- `npm.cmd run build`: passed, 29 modules transformed, built in 515 ms

The previous Codex sandbox failure is retained only as environment evidence:
`evidence/tdd/T003-sandbox-failure.log`.

## External Canonical Quality Gate

Final external canonical gate evidence:

```text
source = USER_EXECUTED_EXTERNAL_ENVIRONMENT
environment = Windows ordinary non-admin PowerShell
privilege_elevation = false

command =
.\backend\.venv\Scripts\python.exe scripts\check_all.py

result = PASS

format = PASS
ruff = PASS
mypy = PASS

backend_pytest =
13 passed
1 existing warning

frontend_type_check = PASS

frontend_vitest =
Test Files 1 passed
Tests 1 passed
```

Log: `evidence/tdd/T003-external-check-all.log`.

## Quality Entrypoint Red

`test_T003_uses_current_python_interpreter`,
`test_T003_propagates_child_failure`,
`test_T003_uses_frontend_working_directory`, and
`test_T003_reports_missing_python_tool_actionably` were added before implementation.

Red result: `REPRODUCED_RED`.

```text
backend\.venv\Scripts\python.exe -m pytest tests\meta\test_quality_commands.py --junitxml=evidence\test-reports\T003.xml -p no:cacheprovider
exit code: 1
4 failed
AttributeError: module 'scripts.check_all' has no attribute 'preflight'
```

Log: `evidence/tdd/T003-quality-entrypoint-red.log`.

## Quality Entrypoint Green

The quality script now:

- uses `sys.executable` for Python checks instead of a bare `python`;
- runs frontend commands from `frontend/`;
- propagates failing child command exit codes;
- preflights required Python and Node tools;
- reports `QUALITY_ENV_MISSING_PYTHON_TOOL` with the current interpreter and canonical command.

Green result:

```text
backend\.venv\Scripts\python.exe -m pytest tests\meta\test_quality_commands.py --junitxml=evidence\test-reports\T003.xml -p no:cacheprovider
exit code: 0
4 passed
```

Log: `evidence/tdd/T003-quality-entrypoint-green.log`.

## Canonical Command

Run from repository root:

```powershell
.\backend\.venv\Scripts\python.exe scripts\check_all.py
```

The user-provided failing command:

```powershell
python scripts\check_all.py
```

used `D:\software\python.exe` and failed with `No module named ruff`; that is now treated as
`NON_CANONICAL_INTERPRETER_INVOCATION` unless the canonical backend venv interpreter fails the same
preflight.

## Final Classification

- Repository quality gate: PASS.
- External ordinary-user execution: PASS.
- Codex sandbox native-child execution: environment-specific limitation.
- Repository defect: NOT PRESENT.
- Privilege escalation: NOT REQUIRED.
- ACL modification: NOT REQUIRED.
- Vitest/Vite rewrite: NOT REQUIRED.

The existing `StarletteDeprecationWarning` is recorded as a third-party dependency warning and is
not a T003 blocker.
