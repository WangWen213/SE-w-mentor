# T003 TDD Evidence

Status: real blocker remains.

Ordinary-permission frontend test command currently fails:

```text
npm.cmd run test -- --run
exit code: 1
error: Cannot read directory "../../..": Access is denied.
error: Could not resolve frontend/vitest.config.mjs
```

Elevated/path-permissive run has passed previously, but that is not accepted as final T003
evidence.

Attempted ordinary-permission mitigations:

- run from `frontend/`,
- run via `npm.cmd --prefix frontend`,
- explicit `--root`,
- explicit `--dir`,
- explicit config path,
- `.mjs` Vitest config,
- fixed `TMP` and `TEMP`,
- disabled cache,
- changed pool to `forks`.

## Additional Diagnosis After T000

Ordinary-permission checks were run from `C:\Users\ww\Desktop\SE-w-mentor\frontend`.

Environment:

```text
Node v24.15.0
npm 11.12.1
cwd C:\Users\ww\Desktop\SE-w-mentor\frontend
TMP/TEMP C:\Users\ww\AppData\Local\Temp
```

Minimal reproductions:

```text
npm.cmd run test -- --run
exit code: 1
Cannot read directory "../../..": Access is denied.
Could not resolve "C:\Users\ww\Desktop\SE-w-mentor\frontend\vitest.config.mjs"
```

```text
node node_modules\esbuild\bin\esbuild vitest.config.mjs --bundle --platform=node --outfile=..\.tmp\vitest-config-bundle.mjs
exit code: 1
Cannot read directory "../../..": Access is denied.
```

```text
node node_modules\esbuild\bin\esbuild .\src\smoke.test.ts --bundle --platform=node --outfile=..\.tmp\smoke-bundle.js
exit code: 1
Cannot read directory "../../..": Access is denied.
```

Root cause range:

- The failure is not limited to Vitest test discovery; direct esbuild bundling also fails.
- Node `fs.statSync()` can read `.`, `..`, `../..`, `../../..`, and the config files from the same
  shell context, so the failing boundary appears when esbuild's native Windows binary runs.
- Vite build still passes, so the failure is specific to this ordinary-permission Vitest/esbuild
  config/test bundling path, not all frontend tooling.

Excluded or reduced hypotheses:

- Not caused only by `vitest.config.ts`; `.mjs` config fails the same way.
- Not fixed by explicit `--root`, `--dir`, or absolute config path.
- Not fixed by moving `TMP`/`TEMP` to the repository `.tmp`.
- Not fixed by disabling cache or switching pool to `forks`.
- Not caused by symlinked config files; checked files are normal files.

Next diagnostic commands:

```text
node node_modules\esbuild\bin\esbuild <entry> --bundle --platform=node --log-level=debug
icacls C:\Users\ww
icacls C:\Users\ww\Desktop
icacls C:\Users\ww\Desktop\SE-w-mentor
```

Current state: T003 remains `[-]`.
