# T008 TDD Evidence

Status: GREEN on branch `codex/T008-migration-governance`.

## PRE_EXISTING_GREEN

The plan's initial single-head repository check was naturally green because the T007 baseline has
exactly one Alembic head.

- Command: `.\backend\.venv\Scripts\python.exe scripts\check_alembic_heads.py`
- Evidence: `evidence/tdd/T008-pre-existing-green.log`
- Result before new T008 tests: exit 0

## RED

New T008 governance tests failed for real missing contract behavior:

- dual-head fixture returned exit 0 instead of failing closed,
- zero-head fixture returned exit 0 instead of failing closed,
- `docs/MIGRATION_POLICY.md` lacked the full ownership/revision/conflict/CI contract,
- `scripts/check_all.py` did not invoke the Alembic head gate.

Evidence: `evidence/tdd/T008-red.log`.

## GREEN

Implemented:

- deterministic `scripts/check_alembic_heads.py` CLI with optional fixture config/cwd,
- fail-closed semantics for 0 and multiple heads,
- detected head count and revision ID output,
- `scripts/check_all.py` integration before pytest,
- full `docs/MIGRATION_POLICY.md` ownership and revision allocation contract,
- `tests/meta/test_migration_policy.py` coverage.

Evidence:

- `evidence/tdd/T008-green.log`
- `evidence/test-reports/T008.xml`
- `evidence/logs/T008/alembic-head-gate.log`
- `evidence/logs/T008/alembic-heads.log`
- `evidence/logs/T008/meta-backend-regression.log`
- `evidence/logs/T008/check-all-sandbox.log`

## Regression Notes

Meta plus backend regression passed 35 tests with one existing third-party
`StarletteDeprecationWarning`. In-sandbox canonical `check_all.py` now runs the Alembic gate,
meta tests, backend tests, and frontend type-check before reaching the pre-existing T003
`CODEX_SANDBOX_NATIVE_CHILD_RESTRICTION` in Vitest/esbuild config loading.
