# T010 TDD Evidence

Status: GREEN on branch `codex/T010-task-domain`.

## Basis

- Requested plan file `SE-Mentor_PLAN_v2_COMPLETE.md` is absent in this checkout; execution used
  `SE-Mentor_PLAN_v2_NO_REVIEW_CLOSURE.md` plus the user's T010 instructions.
- Migration policy owner lane is `wt-schema`; T010 is assigned to that lane and owns
  `0020_task_domain`.
- Current Alembic head before implementation was `0010_project_domain`; T010 revision now advances
  it to `0020_task_domain`.
- T009 Project persistence is the FK boundary for `ChangeTask.project_id`.

## Scope Boundary

Implemented only task-domain persistence:

- `ChangeTask`
- `ChangeProposal`
- `TaskIteration`
- model registry updates
- `0020_task_domain` migration
- model tests and evidence

Not implemented: task services, repositories, REST API, CLI, agent loop, planner/executor/reviewer,
approval workflow, patch generation, memory retrieval, risk evaluation, LLM integration, or Git
mutation workflows.

## Planned RED

The planned test was `test_T010_proposal_v1_cannot_be_overwritten_and_negative_counts_fail`.

Initial result: no `PRE_EXISTING_GREEN`. The test module failed during collection because
`se_mentor.models.task` did not exist.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_task_models.py --junitxml=evidence\test-reports\T010-red.xml
```

Date: 2026-08-07.

Exit code: 2.

Evidence: `evidence/tdd/T010-red.log`.

## Actual RED

Real missing task-domain model:

- `ModuleNotFoundError: No module named 'se_mentor.models.task'`

No task, proposal, iteration, or T010 migration existed yet.

## GREEN

Implemented:

- `ChangeTask` with project ownership, request identity, base revision/workspace hash fields, frozen
  SPEC task status values, counters, budget/active-reference fields, timestamps, and DB check
  constraints.
- `ChangeProposal` with task ownership, positive `version`, JSON text payload fields,
  completeness/status/creator enums, supersedes self-reference, and immutable-version uniqueness.
- `TaskIteration` with task ownership, positive `iteration_number`, SPEC phase/result enums, token
  count metadata, timestamps, progress score, and unique task iteration numbering.
- `0020_task_domain` migration with down_revision `0010_project_domain`.
- ORM/migration parity coverage for indexes, check constraints, and revision lineage.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_task_models.py --junitxml=evidence\test-reports\T010.xml
```

Date: 2026-08-07.

Exit code: 0.

Evidence:

- `evidence/tdd/T010-green.log`
- `evidence/test-reports/T010.xml`

## Regression

Evidence:

- `evidence/logs/T010/t009-t010-model-regression.log`
- `evidence/logs/T010/foundation-meta-backend-regression.log`
- `evidence/logs/T010/alembic-heads.log`
- `evidence/logs/T010/alembic-upgrade.log`
- `evidence/logs/T010/alembic-downgrade.log`
- `evidence/logs/T010/alembic-reupgrade.log`
- `evidence/logs/T010/alembic-gate.log`
- `evidence/logs/T010/ruff-format.log`
- `evidence/logs/T010/ruff.log`
- `evidence/logs/T010/mypy.log`
- `evidence/logs/T010/check-all.log`

Branch validation passed for scoped tests, T009+T010 model regression, meta/backend regression,
Alembic upgrade/downgrade/re-upgrade, Alembic heads, single-head gate, ruff format/check, and mypy.

`scripts/check_all.py` reached the documented in-sandbox Vitest/esbuild access restriction only
after earlier Python, Alembic, backend, and frontend type-check gates passed.

## Worktree Note

Requested `wt-schema` worktree creation failed because the managed sandbox blocked creation of the
Git ref lock under `.git/refs/heads`. No privilege escalation was used. T010 proceeded on isolated
branch `codex/T010-task-domain` from `main @ 8018128`.
