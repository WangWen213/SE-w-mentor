# T011 TDD Evidence

Status: GREEN on branch `codex/T011-llm-action-domain`.

## Basis

- Authoritative plan file: `SE-Mentor_PLAN_v2_NO_REVIEW_CLOSURE.md`.
- The nonexistent `SE-Mentor_PLAN_v2_COMPLETE.md` was not used.
- Migration policy owner lane is `wt-schema`; T011 is assigned to that lane and owns
  `0030_llm_action`.
- Current Alembic head before implementation was `0020_task_domain`; T011 revision now advances it
  to `0030_llm_action`.
- T010 `TaskIteration` is the FK boundary for `LLMCall` and `AgentAction`.

## Scope Boundary

Implemented only LLM/action persistence:

- `LLMCall`
- `AgentAction`
- model registry updates
- `0030_llm_action` migration
- model tests and evidence

Not implemented: governance, impact reports, approvals, execution policy, dispatching, retry engine,
provider abstraction, REST API, CLI, planner/executor/reviewer, tool execution, or T012+ models.

## Planned RED

The planned test was `test_T011_llm_call_requires_model_token_and_parse_status`.

Initial result: no `PRE_EXISTING_GREEN`. The test module failed during collection because
`se_mentor.models.llm` did not exist.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_llm_action_models.py --junitxml=evidence\test-reports\T011-red.xml
```

Date: 2026-08-07.

Exit code: 2.

Evidence: `evidence/tdd/T011-red.log`.

## Actual RED

Real missing LLM/action domain model:

- `ModuleNotFoundError: No module named 'se_mentor.models.llm'`

No `LLMCall`, `AgentAction`, or T011 migration existed yet.

## GREEN

Implemented:

- `LLMCall` bound to `TaskIteration`, with provider/model, bounded redacted summaries,
  `input_tokens`, `output_tokens`, derived `total_tokens`, compression/retry counts, status,
  latency in milliseconds, `error_code`, parse status, and DB check constraints.
- `AgentAction` bound to `ChangeTask` and `TaskIteration`, optionally sourced from `LLMCall`, with
  positive `action_sequence`, frozen action type, bounded `parameters_summary`, optional artifact
  reference, schema version, parse status, risk level, action status, idempotency key, and DB
  constraints.
- Secret/unbounded prompt sink rejection at constructor and attribute assignment boundaries.
- `0030_llm_action` migration based on `0020_task_domain`.
- ORM/fresh migrated DB parity coverage for columns, FKs, indexes, CHECK constraints, and revision
  lineage.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_llm_action_models.py --junitxml=evidence\test-reports\T011.xml
```

Date: 2026-08-07.

Exit code: 0.

Evidence:

- `evidence/tdd/T011-green.log`
- `evidence/test-reports/T011.xml`

## Regression

Evidence:

- `evidence/logs/T011/t009-t010-t011-model-regression.log`
- `evidence/logs/T011/foundation-meta-backend-regression.log`
- `evidence/logs/T011/alembic-heads.log`
- `evidence/logs/T011/alembic-upgrade.log`
- `evidence/logs/T011/alembic-downgrade.log`
- `evidence/logs/T011/alembic-reupgrade.log`
- `evidence/logs/T011/alembic-gate.log`
- `evidence/logs/T011/ruff-format.log`
- `evidence/logs/T011/ruff.log`
- `evidence/logs/T011/mypy.log`
- `evidence/logs/T011/check-all.log`

Branch validation passed for scoped tests, T009+T010+T011 model regression, meta/backend
regression, Alembic upgrade/downgrade/re-upgrade, Alembic heads, single-head gate, ruff
format/check, and mypy.

`scripts/check_all.py` reached the documented in-sandbox Vitest/esbuild directory access
restriction only after earlier Python, Alembic, backend, and frontend type-check gates passed.

## Worktree Note

Requested `wt-schema` worktree creation failed because the managed sandbox blocked creation of the
Git ref lock under `.git/refs/heads`. No privilege escalation was used. T011 proceeded on isolated
branch `codex/T011-llm-action-domain` from `main @ 05f7963`.
