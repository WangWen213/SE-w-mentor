# T004 TDD Evidence

Status: red-green complete after merge-precheck coverage expansion.

## Red

Command:

```text
C:\Users\ww\Desktop\SE-w-mentor\backend\.venv\Scripts\python.exe -m pytest backend\tests\contracts\test_contracts.py --junitxml=evidence\test-reports\T004.xml
```

Result:

```text
exit code: 1
ModuleNotFoundError: No module named 'se_mentor.contracts'
```

The failure was real: the shared contracts package did not exist.

Merge-precheck red result:

```text
exit code: 1
FAILED test_T004_frontend_and_backend_enums_are_consistent
FileNotFoundError: frontend/src/contracts/enums.ts
```

The failure was real: backend contracts existed, but the frontend enum mirror did not.

## Green

Command:

```text
$env:PYTHONPATH=(Resolve-Path backend\src).Path
C:\Users\ww\Desktop\SE-w-mentor\backend\.venv\Scripts\python.exe -m pytest backend\tests\contracts\test_contracts.py --junitxml=evidence\test-reports\T004.xml -p no:cacheprovider
```

Result:

```text
exit code: 0
7 passed
```

## Quality

- Ruff passed for `backend/src/se_mentor/contracts` and `backend/tests/contracts`.
- mypy passed for `backend/src/se_mentor/contracts` and `backend/tests/contracts`.
- `tsc --noEmit --strict frontend/src/contracts/enums.ts` passed.
- JSON Schema snapshots are stored under `backend/tests/contracts/snapshots/`.

## Logs

- Red log: `evidence/tdd/T004-red.log`
- Green log: `evidence/tdd/T004-green.log`

## Commit Provenance

- Initial implementation: `7b839c1`
- Expanded tests and frontend enum mirror: `fd3e775`
- Evidence refresh after rebase: `920fbd4`
- Merge to main: `251170637434c1b8919edd154cad225542cbfaf6`
- Integration metadata: `4d9dd2e30a16b7999fe5758659a8c24b5dc6e35e`

`1184c7beca606a38769ef58cb66d5a453323c294` is the T001 semantic correction and the T004 branch
HEAD after rebase, not a T004 implementation commit.
