# T007 TDD Evidence

Status: branch complete on `codex/T007-db-baseline`; project-level `[x]` requires merge, main
regression, and integration metadata.

## Capability Audit Matrix

| Requirement | Current implementation | Current test | Status | Gap |
| --- | --- | --- | --- | --- |
| SQLAlchemy 2 typed baseline | `Base(DeclarativeBase)` with naming convention | `backend/tests/db/test_session.py` imports and uses engine/session | PASS | None |
| SQLite | `create_sqlite_engine()` creates SQLAlchemy sqlite engines | T007 DB tests use temporary sqlite files | PASS | None |
| `foreign_keys=ON` | connect listener executes real pragma | `test_T007_transaction_rolls_back_and_foreign_key_is_enforced` and pragma test | PASS | None |
| WAL | connect listener executes `PRAGMA journal_mode=WAL` | `test_T007_sqlite_connection_enables_wal_and_busy_timeout` checks real returned value | PASS | Previously unverified |
| `busy_timeout` | connect listener applies caller-provided timeout | same pragma test checks real `PRAGMA busy_timeout` | PASS | Previously hard-coded/unverified |
| transaction exception rollback | `session_scope()` rolls back on exception | original PLAN test | PRE_EXISTING_GREEN | None |
| temporary test database | tests use `tmp_path`; Alembic accepts explicit temp DB URL | DB and Alembic tests | PASS | First Codex run needed worktree-local TMP/TEMP |
| Alembic `upgrade head` | baseline migration exists | subprocess test plus `evidence/logs/T007/alembic-upgrade.log` | PASS | Previously not tied to explicit temp DB |
| Alembic `downgrade base` | baseline downgrade succeeds | subprocess test plus `evidence/logs/T007/alembic-downgrade.log` | PASS | Previously not tied to explicit temp DB |
| T005 formal config integration | DB runtime settings consume `EffectiveConfig` and freeze version/hash | `test_T007_database_settings_are_supplied_by_effective_config` | PASS | Real red: missing function before implementation |
| import without startup side effects | DB modules expose factories; no engine/session created at import | Code review, tests import modules without files until factory use | PASS | None |

## Bootstrap Baseline

The original PLAN test is classified as:

```text
classification = PRE_EXISTING_GREEN
bootstrap_exception = true
```

With worktree-local `TMP/TEMP`, the pre-existing baseline test passed:

```text
backend\tests\db\test_session.py
1 passed
```

Log: `evidence/tdd/T007-pre-existing-green.log`.

The first Codex run hit an environment permission issue creating
`C:\Users\ww\AppData\Local\Temp\pytest-of-ww`; that is retained as
`evidence/logs/T007/codex-temp-fixture-permission.log` and is not treated as a T007 red.

## Real Red

New tests were added for true gaps:

- actual WAL and busy timeout pragma verification;
- DB runtime settings supplied through T005 `EffectiveConfig`;
- Alembic explicit temporary database URL with no `SE_MENTOR_DATABASE_URL` dependency.

Red result:

```text
exit code: 2
ImportError: cannot import name 'database_settings_from_effective_config'
```

Log: `evidence/tdd/T007-red.log`.

## Green

Implementation:

- added `DatabaseRuntimeSettings`;
- added `database_settings_from_effective_config()`;
- made sqlite busy timeout configurable and validated;
- removed Alembic environment-variable URL lookup;
- added explicit `alembic -x database_url=...` support.

Green result:

```text
backend\tests\db\test_session.py
4 passed
```

Log: `evidence/tdd/T007-green.log`; JUnit: `evidence/test-reports/T007.xml`.

## Regression

- Foundation backend/meta regression: 30 passed, 1 existing third-party warning.
- `scripts/check_traceability.py`: 134 P0 requirements mapped.
- `scripts/check_alembic_heads.py`: exit code 0.
- T007 scope Ruff check: pass.
- T007 scope Ruff format: pass.
- backend mypy: pass, 25 source files.

Frontend type-check was not rerun in the T007 worktree because ignored `frontend/node_modules` is
not present in new Git worktrees and T007 did not modify frontend files.

## Evidence

- Pre-existing green: `evidence/tdd/T007-pre-existing-green.log`
- Real red: `evidence/tdd/T007-red.log`
- Green: `evidence/tdd/T007-green.log`
- JUnit: `evidence/test-reports/T007.xml`
- Alembic upgrade: `evidence/logs/T007/alembic-upgrade.log`
- Alembic downgrade: `evidence/logs/T007/alembic-downgrade.log`
- Alembic heads: `evidence/logs/T007/alembic-heads.log`
- Foundation regression: `evidence/logs/T007/foundation-regression.log`
- Ruff: `evidence/logs/T007/ruff.log`
- Ruff format: `evidence/logs/T007/ruff-format.log`
- mypy: `evidence/logs/T007/mypy.log`
- Traceability: `evidence/logs/T007/traceability.log`
