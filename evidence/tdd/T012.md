# T012 TDD Evidence

Status: GREEN on branch `codex/T012-governance-domain`.

## Basis

- Authoritative plan file: `SE-Mentor_PLAN_v2_NO_REVIEW_CLOSURE.md`.
- T011 was complete and pushed to `origin/main` before T012 started.
- Current Alembic head before implementation was `0030_llm_action`; T012 revision now advances it
  to `0040_governance`.
- T012 covers persistence for `ImpactReport`, `GovernanceDecision`, `GovernanceRule`, and
  `GovernanceRuleHit` as support for `FR-04` and `FR-06`.

## Scope Boundary

Implemented only impact analysis and governance persistence:

- `ImpactReport`
- `GovernanceDecision`
- `GovernanceRule`
- `GovernanceRuleHit`
- model registry updates
- `0040_governance` migration
- model tests and evidence

Not implemented: approvals, approval requests, execution policy, temporary grants, policy
evaluation, governance engine, execution gating, action classification, human approval workflow,
REST API, CLI, UI, or T013+ models.

## Planned RED

The planned test was `test_T012_deny_hard_rule_cannot_be_overridable`.

Initial result: no `PRE_EXISTING_GREEN`. The test module failed during collection because
`se_mentor.models.governance` did not exist.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_governance_models.py --junitxml=evidence\test-reports\T012-red.xml
```

Date: 2026-08-08.

Exit code: 2.

Evidence: `evidence/tdd/T012-red.log`.

## Actual RED

Real missing governance domain model:

- `ModuleNotFoundError: No module named 'se_mentor.models.governance'`

No governance ORM model or T012 migration existed yet.

## GREEN

Implemented:

- `ImpactReport` bound to task and proposal, with base revision, direct impact JSON, optional
  category impact JSON fields, uncertainty JSON, evidence JSON, status, timestamps, FKs, checks,
  and indexes.
- `GovernanceDecision` bound to task, optional action, optional impact report, `proposal_hash`,
  `revision`, verdict, risk level, scope summaries, approval flag, status, ruleset version,
  evidence JSON, and expiry.
- `GovernanceRule` with stable `rule_key`, `rule_name`, scope, effect, priority, separate
  `patterns_json` and `conditions_json`, reason, overridable flag, enabled flag, exact
  `rule_version`, checks, and indexes.
- `GovernanceRuleHit` bound to a decision and the exact rule row/version through a composite FK,
  with duplicate decision/rule-version hit prevention.
- Hard-DENY invariant: `DENY_HARD` rules cannot be `overridable`.
- Nonexistent rule rejection through the `GovernanceRuleHit(rule_id, rule_version)` FK.
- ORM/fresh migrated DB parity coverage for columns, FKs, indexes, CHECK constraints, and revision
  lineage.

Command:

```text
.\backend\.venv\Scripts\python.exe -m pytest backend\tests\models\test_governance_models.py --junitxml=evidence\test-reports\T012.xml
```

Date: 2026-08-08.

Exit code: 0.

Evidence:

- `evidence/tdd/T012-green.log`
- `evidence/test-reports/T012.xml`

## Regression

Evidence:

- `evidence/logs/T012/t009-t010-t011-t012-model-regression.log`
- `evidence/logs/T012/foundation-meta-backend-regression.log`
- `evidence/logs/T012/alembic-heads.log`
- `evidence/logs/T012/alembic-upgrade.log`
- `evidence/logs/T012/alembic-downgrade.log`
- `evidence/logs/T012/alembic-reupgrade.log`
- `evidence/logs/T012/alembic-gate.log`
- `evidence/logs/T012/traceability.log`
- `evidence/logs/T012/ruff-format.log`
- `evidence/logs/T012/ruff.log`
- `evidence/logs/T012/mypy.log`
- `evidence/logs/T012/check-all.log`
- `evidence/logs/T012/check-all-escalated.log`

Branch validation passed for scoped tests, T009+T010+T011+T012 model regression, meta/backend
regression, Alembic upgrade/downgrade/re-upgrade, Alembic heads, single-head gate, traceability,
ruff format/check, and mypy.

In-sandbox `scripts/check_all.py` reached the documented Vitest/esbuild directory access
restriction only after format, lint, mypy, Alembic gate, meta tests, backend tests, and frontend
type-check passed. A requested non-sandbox retry then failed earlier because Windows refused to
delete an existing `.tmp/check-all/*-pytest-basetemp` directory; direct deletion of those generated
directories was also denied by the host environment.

## Worktree Note

Requested `wt-schema` worktree creation failed because the managed sandbox blocked creation of the
Git ref lock under `.git/refs/heads`. T012 proceeded on isolated branch
`codex/T012-governance-domain` from `main @ b10997f`.
