# Reflection

完成 SE-Mentor 这个项目之后，我最大的感受并不是“用 AI 把一个项目做出来了”，而是第一次比较完整地经历了一个 harness的项目设计开发。从最开始的需求分析、功能规约和架构设计，到后面的任务拆分、代码实现、测试、前后端联调，再到最后真正把系统跑起来并不断修复实际使用中的问题，我对 Coding Agent 的能力、局限以及人与 AI 在软件工程中的分工，相比于做一个软工项目又多了一点反思吧。

## 1. Todo 和 Task 可以推动项目向前，但“完成任务”并不等于“完成产品”

整个项目开发过程中，基本按照老师提供的 Todo 框架，把一个比较大的系统不断拆分成具体的 Task，再按照 Task 顺序向前推进，确实可以有计划地推进。

如果没有这样的任务框架，会容易一开始就把注意力放在最终页面或者某一个功能上比如之前的软工作业会扣着像橱窗的一个小功能，然后边写边改，最后发现数据模型、状态流转、治理机制或者接口之间存在大量不一致。而通过 Task 拆分之后，项目被分成了数据模型、项目理解、Proposal、Impact、Governance、Execution、Validation、Memory、Web UI 等多个阶段，每个阶段都有相对明确的目标。

尤其是在和 Codex 配合时，这种开发方式和指令非常有效，相比于直接告诉 AI “帮我做一个 SE-Mentor”，告诉它当前只完成一个具体 Task，它能够更准确地理解修改范围，也更容易控制代码改动不会影响其他已经完成的功能。

但随着项目进入后期，我逐渐发现一个非常明显的问题：

**Task 被验收，不代表这个功能在真实系统中已经可以使用。**

前期很多任务从测试结果来看已经完成了。数据模型存在、接口可以调用、状态能够持久化，甚至对应测试全部通过。但真正启动前后端，把它当成一个普通用户实际操作的时候，问题会一下子暴露出来。

例如项目打开之后是否能够稳定恢复之前的项目，Proposal 生成以后页面是否立刻更新，点击确认之后 Governance 是否能够正常出现，刷新页面后状态是否还保持一致，这些问题并不是某一个孤立 Task 的测试就能够完全覆盖的。

有些功能在代码层面已经是“正确”的，但是在人真正使用时仍然是不完整的。

这让我意识到软件工程里面存在一个很重要的距离：

**Implementation → Test → Acceptance → Real Use**

这几层实际上不是一回事，Task 列表全部变成 `[x]` 无法成为项目完成的标准。

## 2. 前端让我最明显地感受到“正确”和“好用”之间的差距

这个项目里，我感觉最难修改的部分其实不是数据模型，也不是后端接口，而是前端，尤其是 **UI 层和前端核心状态层**。

最开始我认为前端更多是“把后端已有的数据展示出来”，真正做以后发现完全不是这样。

页面是否稳定、刷新以后恢复什么状态、任务正在执行的时候显示什么、Proposal 确认之后原来的卡片是否保留、Governance 的加载状态如何展示、输入框什么时候应该可用，这些全部涉及大量状态之间的关系。

而且前端还有一个后端比较少遇到的问题：

**它不仅需要逻辑正确，还需要让用户觉得正确。**

例如一个状态实际上已经在后台执行，但如果页面几秒钟没有任何变化，用户就会认为系统卡住了；如果刷新以后先显示“无项目”，几秒以后又恢复原来的项目，即使最终数据正确，体验上仍然是不稳定的；如果 Governance 已经生成，但页面必须刷新以后才能出现，从接口角度看可能没有错误，但从产品角度看这个功能就是有问题。

UI 的修改也是类似的。

很多时候一个页面从代码和功能上已经满足要求，但实际看起来仍然会觉得信息密度太高、层级不清楚、按钮位置奇怪，或者页面不像一个真正可以长期使用的软件。

也正因为这样，前端最终还是需要大量人工观察和判断。AI 可以帮助修改组件、整理状态、调整布局，但“这个页面现在到底舒不舒服”“这个交互是不是符合人的直觉”，目前仍然非常依赖我们人在真实使用中的感受。


## 3. AI 对“边界”的把控，比我原本预期的价值更大

SE-Mentor 本身是一个比较强调工程安全和治理的项目，因此整个开发过程中经常需要考虑一个问题：

**一个修改到底可以改到哪里？**

这一点是我做完项目之后对 AI 能力感受比较深的地方。

如果完全让我自己设计某一项安全规则，我可能可以想到一些比较明显的条件，例如不允许修改敏感文件、危险操作需要确认、执行前进行 Impact Analysis 等。

但是实际系统里的边界往往比这些复杂得多。

一个改动可能本身没有直接修改敏感文件，但是它会影响某个依赖；一个操作本身没有问题，但在某种项目状态下就不应该执行；某些规则应该直接 DENY，有些应该 WARN，有些应该 REQUIRE_APPROVAL。

这时候 AI 的作用并不是简单地“替我做决定”，而是能够帮助我补全原来没有想到的情况。

我自己可以提供最核心的目标和原则，而 AI 可以沿着这些原则继续分析：

- 还有哪些异常路径？
    
- 哪些输入没有考虑？
    
- 哪些状态组合存在风险？
    
- 哪些操作应该属于硬边界？
    
- 哪些地方应该允许用户覆盖？
    

这一点其实和 SE-Mentor 自身的理念非常接近。

我现在更倾向于认为，在高安全要求的软件工程场景中，AI 一个非常重要的价值并不是完全自动完成开发，而是作为一种 **边界检查器和第二视角**。

人很容易因为已经熟悉自己的设计，而默认很多条件成立；AI 反而可以从另一个角度不断追问这些默认条件是否真的成立。

当然，最终边界仍然必须由人决定。因为“什么风险可以接受”本质上还是一个工程决策，而不仅仅是代码判断。


## 4. 我对 Codex 这种 Coding Agent 的认识发生了很大变化

做这个项目之前，我对 Coding Agent 的理解更多还是“比代码补全更强的 AI”。

在自己去做一个agent开放流程之后，我觉得这个理解已经不太准确了。

从这次实际操作来看，它已经可以参与非常完整的软件工程循环：

**理解需求 → 阅读代码 → 分析问题 → 制定修改方案 → 修改多个文件 → 根据现有架构继续实现 → 根据真实运行结果再次定位问题。**

尤其是当项目本身已经有比较明确的 Task、规范和架构之后，Codex 的表现会明显更好。

它不仅可以完成某一段代码，而是能够沿着已有工程结构工作。相比单纯在聊天窗口里面让 AI 生成代码，Codex 更接近“AI 真的坐在这个代码仓库里面工作”。它可以围绕具体代码文件、修改内容和任务持续推进，而不是每一次都重新解释整个项目背景。

实际对比下来，这种体验非常人性化。

尤其是在大型修改中，最重要的其实不是一次生成多少代码，而是：

**能不能连续地理解项目，并且在一次又一次修改之后仍然知道自己正在解决什么问题。**

从这一点来说，我觉得 Codex 已经不仅仅是一个代码生成工具，而是一个比较成熟的 Coding Agent。相比而言，自己开发的就显得太简陋了。
